{
  "name": "@cal/domain",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cal/schemas": "workspace:*",
    "@cal/types": "workspace:*"
  },
  "devDependencies": {
    "@cal/config": "workspace:*",
    "typescript": "~5.9.2",
    "vitest": "^2.1.2"
  }
}
